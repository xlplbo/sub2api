# 撤销分组 Codex 仅 WS 接入限制

撤销提交 `669cdca0cd0a05fc3076b235e146dbe61308beb4` 的应用行为和管理界面，恢复此前的 Codex HTTP / WS 接入规则。此次撤销不调整账号调度、WS 并发等待或上游传输模式。

## 数据库

- 保留已发布的 `239_group_codex_ws_only.sql` 原始内容和迁移记录，避免修改历史校验和。
- 新增 `240_revert_group_codex_ws_only.sql` 补偿迁移：先恢复迁移 193 的分组认证缓存失效函数，再删除 `groups.codex_ws_only`。
- 已升级数据库只执行 240；新数据库按顺序执行 239、240，最终结构相同。240 可重复执行。
- 用户、余额、API Key、账号、用量和其他分组字段均保留。不要恢复旧整库备份，也不要单独删除字段或迁移记录。
- 恢复 v24 认证快照后，旧进程留下的 v25 缓存会回源重建，无需清空 Redis。

## 部署顺序

1. 完成前后端构建，在隔离 PostgreSQL schema 中验证升级、重复执行、触发器行为及数据保留。
2. 备份当前程序、配置和数据库。停止旧应用后再执行清理，避免仍使用该字段的旧进程访问新结构。
3. 在事务中执行 240，并按迁移执行器规则记录完整文件名与去除首尾空白后的 SQL SHA256；也可由已替换的回退版本在启动时自动执行。
4. 部署回退版本，检查服务健康、迁移记录、字段删除、触发器定义、分组数据读取及 HTTP / WS 接入。

## 部署失败时

若需重新启用含该字段的旧二进制，必须先恢复字段和迁移 239 的触发器，并恢复备份的各分组开关值，之后才能启动旧程序。不能将仍含此功能的程序直接连接到已删除该字段的数据库。失败恢复应记录到部署审计中，不使用整库恢复覆盖新的业务数据。
